# Appwrite Configuration
# Get these values from your Appwrite Console: https://cloud.appwrite.io/console

# Your Appwrite Project ID (required)
# Found in: Appwrite Console → Project Settings → Project ID
APPWRITE_PROJECT_ID=your-project-id-here

# Appwrite API Key (required for server-side operations)
# Found in: Appwrite Console → Project Settings → API Keys
# Required scopes: users.read, users.write, sessions.write
APPWRITE_API_KEY=your-api-key-here

# Appwrite API Endpoint (required)
# Default for Appwrite Cloud (EU): https://fra.cloud.appwrite.io/v1
# Default for Appwrite Cloud (US): https://us.cloud.appwrite.io/v1
# For self-hosted: https://your-domain.com/v1
APPWRITE_ENDPOINT=https://fra.cloud.appwrite.io/v1

# GitHub OAuth Configuration (required for GitHub login)
# Create OAuth App at: https://github.com/settings/developers
# Authorization callback URL: http://localhost:3000/api/auth/callback/github (dev)
# Production callback URL: https://your-domain.com/api/auth/callback/github

# GitHub OAuth Client ID
GITHUB_CLIENT_ID=your-github-client-id

# GitHub OAuth Client Secret
GITHUB_CLIENT_SECRET=your-github-client-secret

# Application URL (optional, defaults to http://localhost:3000 in development)
# Used for password reset email links and OAuth callbacks
NUXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: toggle verbose auth logging in server (for local dev only)
# DO NOT enable in production
AUTH_DEBUG=false
